{
  "name": "app",
  "description": "test apps for published packages",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@hellocoop/express": "*",
    "@hellocoop/fastify": "*",
    "@hellocoop/nextjs": "*",
    "express": "^4.18.2",
    "fastify": "^4.25.2"
  }
}
