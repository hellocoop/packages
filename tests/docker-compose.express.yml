# base file -- will not run by itself -- must be extended with either docker-compose.dev.yml or docker-compose.local.yml

version: '3.8'

networks: 
  mock:

services:

  app:
    environment:
      - NODE_ENV=production
    command: ["node", "./express.js"]

  playwright:
  #   # command: ["/bin/sh"]
  #   # command: ["npx", "playwright", "test", "tests/server.test.js"]    
    command: ["npx", "playwright", "test"]